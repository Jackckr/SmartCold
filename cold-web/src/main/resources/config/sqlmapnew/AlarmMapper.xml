<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartcold.manage.cold.dao.newdb.AlarmMapper">


     <select id="getSumValueByFilter" parameterType="map" resultType="ColdStorageAnalysisEntity">
	  select SUM(`value`)`value` ,`date` from `coldstorageanalysis`
		where `type` = #{type} 
		<if test="keys !=null">AND `key` in (${keys})</if>
		<if test="oids !=null">AND `oid` in (${oids}) </if>
		<if test="startTime != null and endTime!=null" >  AND `date` >= #{startTime} AND `date` <![CDATA[ <= ]]> #{endTime}  </if>
		GROUP BY `date`;
	</select>
	
	
	
</mapper>