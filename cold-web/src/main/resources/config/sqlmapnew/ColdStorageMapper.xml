<?xml version="1.0" encoding="UTF-8"?>
		<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartcold.manage.cold.dao.newdb.NewColdStorageMapper">
	<select id="findIBlowerByTime" parameterType="map" resultType="NewColdStorageEntity">
		SELECT *
		FROM `ColdStorage`  WHERE
		  oid=#{oid}  
		AND	`key` = #{key}
		AND `value` > #{iunbalance}
		AND `addtime` > #{startTime}
		ORDER BY
			`addtime` ASC;
	</select>
</mapper>