<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartcold.manage.cold.dao.olddb.ACLMapper" >
	<select id="getUserACL" parameterType="Integer" resultType="String">
	  select nacl from acl_user where uid=#{uid};
	</select>
	<select id="getRDCACL" parameterType="Integer" resultType="String">
	 SELECT nacl from acl_rdc where rdcid=#{rdcid};
	</select>
	<select id="getRUACL" parameterType="Integer" resultType="String">
	  select CONCAT_WS(',',(SELECT nacl from acl_user u where uid=#{uid}) ,(select nacl from acl_rdc where rdcid=#{rdcid} )) as c;
	</select>
	<select id="getALLACLNode" parameterType="map" resultType="com.smartcold.manage.cold.entity.newdb.ACLTreeNode">
	  SELECT m.* ,  if(find_in_set(m.id,#{nacl})>0 or find_in_set(m.pid,#{nacl})>0,0,1)  as acl from coldmenus m  ;
	</select>
	<select id="getACLNodeByPid" parameterType="map" resultType="com.smartcold.manage.cold.entity.newdb.ACLTreeNode">
	  SELECT m.* ,  if(find_in_set(m.id,#{nacl})>0 or find_in_set(m.pid,#{nacl})>0,0,1)  as acl from coldmenus m where pid=#{pid} ;
	</select>
	
	
	
</mapper>