<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.smartcold.zigbee.manage.dao.DataAnalysisMapper">
   <select id="getDataAnalysisBykey" parameterType="map" resultType="map">
     select `value`,DATE_FORMAT(`date`,'%Y.%m') `date`  from dataanalysis where  `type` = #{type}  AND `key`=#{key}  AND `date` >= #{startTime} AND `date` <![CDATA[ <= ]]> #{endTime};
   </select>
</mapper>