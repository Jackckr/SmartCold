<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smartcold.zigbee.manage.dao.MessageRecordMapping">
    <insert id="insertMessageRecord" parameterType="MessageRecord">
        INSERT INTO `messagerecord`
        (`uid`,`rdcid`,`type`,`stype`,`title`,`message`,`addtime`)
        VALUES
        (#{uid},#{rdcId},#{type},#{sType},#{title},#{message},#{addTime})
    </insert>
    <select id="selByUidRdcId" resultType="com.smartcold.zigbee.manage.entity.MessageRecord">
        SELECT * FROM `rdcauth` WHERE `uid`=#{uid} and `rdcid`=#{rdcId}
    </select>
</mapper>